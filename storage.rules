rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    // 1. Completion photos upload by workers
    match /completions/{bookingId}/{allPaths=**} {
      // Anyone can upload job completion photos (with size limit < 5MB and image type)
      allow write: if request.resource.size < 5 * 1024 * 1024 && request.resource.contentType.matches('image/.*');
      // Anyone can read completion photos
      allow read: if true;
    }

    // 2. Service logos upload by admins
    match /logos/{serviceId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.resource.size < 2 * 1024 * 1024 && request.resource.contentType.matches('image/.*'); // limit to 2MB and image type
    }

    // 3. Default rule
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
